"use strict" // treat all JS code as newer version (but now done auto.)

// alert(3 + 3) // we are using nodejs not browser

//ensure code readability should be high

let name = "Piyush"
let age = "21"
let loggedIn = false;

// number => 2^53 - 1
// BigInt
// string => ""
// boolean => true/false
// null => standalone value
// undefined
// symbol => unique

// object

console.log(typeof undefined) //undefined
console.log(typeof null) //object